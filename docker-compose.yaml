# Modular compose — each module is self-contained.
# Comment any line to disable that component.
include:
  - path: ./01_DOCKER_POSTGRESQL/docker-compose.yaml  # pgdatabase (5432) + pgadmin (8085)
  - path: ./02_WORKFLOW_ORCHESTRATION/docker-compose.yaml  # kestra (8080, 8081) + kestra_postgres
  - path: ./04_AIRFLOW/docker-compose.yaml  # airflow (8083) + airflow-postgres + airflow-redis
